html
	head
		title File Upload test
	body
		h1 File upload test
		form(action="/" method="post" enctype="multipart/form-data")
			input(type="file" name="file")
			div
			input(type="submit")

		ul
			for val in files
				li: a(href='/' + val) #{val}